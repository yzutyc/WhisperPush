name: whisper_push
description: WhisperPush message push client
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  shared_preferences: ^2.2.2
  flutter_markdown: ^0.7.7+1
  flutter_html: ^3.0.0-alpha.6
  provider: ^6.1.1
  intl: ^0.20.2
  share_plus: ^7.2.2
  package_info_plus: ^4.2.0
  particles_flutter: ^2.0.2
  visibility_detector: ^0.4.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
